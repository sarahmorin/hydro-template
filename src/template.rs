use hydro_lang::*;

/// Write a function that generates a hydroflow given a process
///
/// ```rust
/// pub fn first_ten(process: &Process) {
//     process
//         .source_iter(q!(0..10))
//         .for_each(q!(|n| println!("{}", n)));
// }
// ```

pub fn flow_template(process: &Process) {
    todo!("Write some hydro code")
}
